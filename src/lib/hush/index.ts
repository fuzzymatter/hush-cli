
/**
 * Starts the signup process
 * @param email 
 */
export const signup = (email:string) => {
  // stub
  return Promise.resolve({
    id: 'vaj89ei4ad',
    timeRemaining: {
      minutes: 5,
      seconds: 20,
    },
  });
};

/**
 * 
 * @param email 
 * @param code 
 */
export const isVerificationCodeValid = (email:string, code:string) => {
  // stub
  return Promise.resolve();
};